# React-admin-system
## launch the project
### Step 1 : start server
npm install -g json-server

open terminal where da.json at

json-server --watch .\db.json --port 5000
### Step 2 : start client

npm start

